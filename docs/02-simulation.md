# 02. Модель симуляции

Приложение к [PRD.md](../PRD.md). Описывает, как схема превращается в числа.

Принцип: **всё, что показывает симулятор, должно быть объяснимо одной формулой с подставленными
значениями.** Никаких «чёрных ящиков» — иначе тренажёр не учит, а гадает.

---

## 1. Общая схема расчёта

```
Scheme + Flows + Scenario
        │
        ▼
[1] TopologyCompiler ──► валидация, SCC, порядок обхода, дерево вызовов на каждый Flow
        │
        ▼
[2] Flow Solver (итерации до сходимости) ──► λ по узлам и рёбрам (read/write, по Flow)
        │            ▲
        │            └── обратная связь: ретраи и деградация меняют λ
        ▼
[3] Capacity Kernel ──► capacity, boundBy, ρ по каждому узлу
        │
        ▼
[4] Queueing ──► W_q, queueDepth, errorRate, throughput
        │
        ▼
[5] Monte-Carlo Latency Roll-up ──► p50/p95/p99 на каждый Flow, водопад по хопам
        │
        ▼
[5а] ConsistencyModel ──► частота аномалий по типам (режим по умолчанию — включён)
        │
        ▼
[6] Derived: storage / memory / logs / egress / cost / availability / RPO / RTO
        │
        ▼
[7] RuleEngine ──► Findings (bottleneck, SPOF, antipattern, SLO violation, anomaly)
```

Шаги 2–4 повторяются до сходимости (демпфированные итерации), потому что ретраи и сброс нагрузки
меняют входной поток. Шаги 5–7 выполняются один раз по сошедшемуся состоянию.

**Стоимость расчёта:** шаги 1–4 — O(V+E) на итерацию, ≤ 50 итераций. Шаг 5 — N сэмплов × глубина
дерева вызовов (N = 20 000 по умолчанию, 4 000 в режиме live-preview).

---

## 2. Шаг 1. Компиляция топологии

Аналог `GraphCompiler` в dsp-flow, но с принципиальным отличием: **циклы легальны**.

| Проверка | Действие |
|---|---|
| Совместимость портов и протоколов | Ошибка: «Клиент не может напрямую говорить по протоколу `sql` с Postgres» (настраиваемо) |
| Висящие рёбра, дубликаты | Ошибка |
| Компоненты связности (union-find) | Предупреждение «3 несвязанные подсхемы» |
| Узлы без входящего трафика | Предупреждение «блок не используется» |
| Циклы | **Не ошибка.** Ищем SCC (Тарьян); внутри SCC решаем итеративно, между SCC — топологический порядок |
| Дерево вызовов на Flow | Обход от `entryNodeId` по sync-рёбрам; async-ребро обрывает дерево (см. §5.4) |
| Глубина синхронной цепочки | Предупреждение при depth > 5 |

Результат — `CompiledTopology`: порядок обхода, дерево вызовов на каждый Flow, карта смежности,
список SCC.

---

## 3. Шаг 2. Распространение потока (Flow Solver)

### 3.1. Базовое уравнение

Для ребра `e: i → j` и класса трафика `κ` (Flow × {read, write}):

```
λ_e,κ  =  λ_i,κ · share_e · fanout_e · (1 − absorb_i,e)
λ_j,κ  =  Σ_e∈in(j) λ_e,κ
```

| Член | Смысл | Кто задаёт |
|---|---|---|
| `share_e` | Доля трафика, уходящая в это ребро при ветвлении (сумма по группе = 1) | Балансировщик — поровну; сервис — вручную |
| `fanout_e` | Сколько вызовов downstream на один входящий запрос (может быть > 1: батч, fan-out на N шардов) | Параметр ребра |
| `absorb_i,e` | Доля, поглощённая узлом `i` (попадание в кэш, ответ из локального кэша, 304 Not Modified) | Производное (Zipf-модель кэша) |

Клиентские блоки инжектируют начальный λ по своим Flow.

**Наследование смеси чтение/запись.** `share_e` профиля вызова — это доля запросов источника, но
для операций доступа к данным (`read`, `scan`, `write`, `delete`) она не берётся с ребра буквально:
ребро наследует смесь вызывающего и делит между профилями своего класса. Для ребра `e` с суммами
долей `W_read` и `W_write` по классам и полной долей доступа `W = W_read + W_write`:

```
share_e,read  = readShare_i  · W · share_call / W_read
share_e,write = writeShare_i · W · share_call / W_write
```

Здесь `readShare_i` — доля чтений в том, что пришло в узел `i` (у клиента это его `readWriteMix`).
Смысл: доли профилей на ребре задают, **что** ребро делает (только чтения — в реплику, только
записи — в primary) и **какая часть** запросов вообще уходит по этому ребру (`W`), а пропорцию
чтений и записей приносит вызывающий. Отсюда три следствия:

* `readWriteMix` клиента виден на всей цепочке: изменили смесь у клиента — изменились рост данных,
  IOPS-нагрузка базы и ставки аномалий;
* разделение чтения и записи на два ребра (реплика и primary) не удваивает поток: сумма по двум
  рёбрам равна потоку источника;
* сохраняется и «сколько» — ребро с `W = 0.3` несёт 30% запросов источника независимо от смеси.

Наследовать нечего там, где во входящем потоке нет операций доступа: консьюмер, которому приходят
только `consume`, ничего не знает про чтения и записи, и его исходящие рёбра берут свои доли
буквально. Так же ведёт себя ребро с `mixMode = manual` — режим «вручную» включается сам, как
только игрок двигает ползунок доли в инспекторе (см. `docs/03-connections.md` §3.1).

### 3.2. Усиление ретраями (retry amplification)

```
λ_offered_j = λ_nominal_j · (1 + A_retry_j)
A_retry_j   = Σ_{k=1..R} min(p_fail_j, retryBudget)^k
```

Где `p_fail_j` — вероятность отказа/таймаута на узле `j`, которая сама зависит от `λ_offered_j`.
Отсюда — **петля обратной связи**: перегруженный сервис получает ещё больше нагрузки от ретраев.
Это моделирует **retry storm** — один из самых поучительных эффектов (и один из сценариев батареи).

Если у ребра `circuitBreaker = true`, то при `p_fail > порога` `A_retry → 0` и трафик обрывается на
источнике (`fail fast`) — сравнение схем с брейкером и без наглядно показывает его смысл.

### 3.3. Итерационный решатель

```
потоки рёбер λ_e ← 0
повторять n = 1..50:
    для каждого узла в порядке конденсации:
        λ_j ← λ_вход,j + Σ_e∈in(j) λ_e          # λ_e форвардных рёбер уже пересчитаны в этом проходе,
                                                # обратных — взяты с предыдущего (Гаусс — Зейдель)
        пересчитать ρ, p_fail, absorb по λ_j
        λ_j ← (1 − ω)·λ_jⁿ⁻¹ + ω·λ_j            # демпфирование, ω = 0.5
        обновить λ_e исходящих рёбер узла
    если ‖λⁿ − λⁿ⁻¹‖∞ / ‖λⁿ‖∞ < 0.0001 → выход
```

Ключевое здесь — **потоки рёбер живут между проходами**. Внутри сильно связной компоненты (цикла)
обратное ребро на первом проходе ещё не посчитано, но на втором его вклад приходит в узел наравне с
прямыми. Без этого обратное ребро терялось бы в каждом проходе, а результат зависел бы от того, в
каком порядке узлы компоненты лежат в массиве.

Цикл синхронных вызовов усиливает трафик: каждый оборот добавляет к нагрузке узлов ещё один заход.
Сходится он там, где ёмкость конечна — поток упирается в неё, узлы получают `overloaded`, и в этом и
состоит урок. Расходимость (λ растёт неограниченно) — это **не баг, а результат**: «схема
неустойчива, ретраи или контур образуют лавину». Симулятор в этом случае помечает Finding
`retry-storm` и показывает точку срыва.

### 3.4. Гео-маршрутизация и мультирегион

Мультирегион входит в MVP (решение **D1**), поэтому распространение потока двухступенчатое:
сначала клиенты распределяются по регионам, затем внутри региона работает обычный решатель.

**Шаг 1 — распределение по географии.** Flow объявляет `clientGeo` — распределение по гео-зонам
(`na-east`, `na-west`, `eu-west`, `eu-central`, `apac-se`, `apac-ne`, `sa`, `me`, `africa`, `oceania`).
Блок `dns`/`glb` по своей `routingPolicy` отображает зону в регион:

```
λ_region,flow = λ_flow · Σ_zones share(zone) · route(zone → region)

route =  latency   → ближайший здоровый регион по матрице RTT
         geo       → явная карта зона→регион (data residency)
         weighted  → заданные веса
         failover  → primary, пока жив; иначе следующий по списку
```

**Шаг 2 — разделение по операциям.** В зависимости от `multi-region-policy.mode`:

| Режим | Чтения | Записи |
|---|---|---|
| `single` | все в единственный регион | туда же |
| `active-passive` | в активный регион | в активный регион |
| `active-active` | локально | локально (→ источник конфликтов) |
| `read-local-write-global` | локально | все в `writeRegion` (+ межрегиональный RTT к latency записи) |
| `sharded-by-geo` | локально по «домашнему» региону ключа | туда же; «чужие» ключи — с межрегиональным хопом |

**Как это устроено в коде** (`routing.ts`). Маршрутизация включается, когда на схеме два и более
региона, а у блока есть параметр `routingPolicy` (`dns`, `glb`) и его исходящие ветви ведут в разные
регионы. Иначе всё остаётся как было — деление по весам рёбер.

* Зона клиента берётся из `geoDistribution` блока-клиента; `global` раскладывается поровну по шести
  зонам. Учитываются только те клиенты, из которых маршрутизатор достижим по графу.
* `latency` — зона уходит в регион с наименьшим RTT по матрице §11.1; `geo` при
  `geoMapping ≠ none` — сначала в регион того же континента, остальное по RTT; `failover` — всё в
  регион с `isPrimary`, а если он выключен сценарием, в следующий живой; `weighted` и `simple`
  оставляют веса рёбер нетронутыми.
* Регион, выключенный сценарием (`region-failure`), выпадает из выбора, и его доля переезжает
  к живым — раньше это работало только через веса.
* Доли считаются **раздельно для чтений и записей**: ребро несёт свою долю чтений и свою долю
  записей, поэтому режим «читаем локально, пишем в один регион» выражается прямо в потоках.

**Шаг 2 в коде.** `mode` берётся с блока `multi-region-policy`; если его на схеме нет, регионы
работают как `active-active`. `single` и `active-passive` уводят и чтения, и записи в активный
регион (`writeRegion` по коду региона, иначе регион с `isPrimary`), `read-local-write-global`
оставляет чтения локальными и уводит записи, `active-active` и `sharded-by-geo` держат всё
локально. Межрегиональный RTT к записи добавлять отдельно не нужно: ребро в чужой регион уже
скомпилировано со `scope = cross-region`, и задержка приходит из топологии. Отличие
`sharded-by-geo` от `active-active` — «чужие» ключи с лишним хопом — не смоделировано.

**Шаг 3 — трафик репликации.** Порождается автоматически, а не рисуется руками:

```
λ_repl = λ_write,локальные,region · (числоРегионов − 1) · directionFactor
байт/с = λ_repl · recordBytes · (1 + overhead)
стоимость = байт/мес · costPerGb(link-cross-region)
```

`λ_write,локальные` — записи, пришедшие в узел **не** по репликационному ребру. Реплицированная
запись не реплицируется дальше: иначе при двунаправленной паре рёбер записи ходили бы по кругу
между регионами и нагрузка баз росла бы на пустом месте. В реальных системах ту же роль играет
фильтр по origin/server-id.

**Зеркальные регионы.** Регион с `mirrorOf` наследует все узлы, рёбра и параметры прототипа;
собственным у него остаётся только `trafficShare` и результаты симуляции. Это ключевое требование
удобства: без него нарисовать три региона вручную — заведомо неподъёмная работа, и мультирегион в
MVP не имел бы смысла. Реализовано материализацией: копии — настоящие узлы с id `<узел>@<регион>`,
их держит в согласии `services/mirrorRegions.ts` (ADR-13 в `docs/05-architecture.md` объясняет, почему
не разворачиванием внутри движка). Для модели это обычные узлы: маршрутизация, ёмкость, стоимость и
аномалии считаются им так же, как нарисованным руками.

**RPO и RTO** считаются здесь же:

```
RPO_actual = replicationLag.p99                       (сколько данных теряется при потере региона)
RTO_actual = detectionSec + dnsTtlSec + failoverSec + warmupSec
```

`dnsTtlSec` часто оказывается доминирующим слагаемым — и это один из самых полезных неочевидных
уроков тренажёра.

---

## 4. Шаг 3. Ядро ёмкости (Capacity Kernel)

Единая формула для всех блоков — только набор слагаемых различается:

```
capacity_i = min over resources r of  limit_r
```

| Ресурс `r` | Формула `limit_r` | Для кого характерен |
|---|---|---|
| Воркеры/CPU (закон Литтла) | `instances · concurrencyPerInstance / S` | Сервисы, воркеры |
| Явный потолок | `instances · maxRpsPerInstance` | API GW, LB, managed-сервисы |
| Соединения | `maxConnections · instances / connectionsPerRequest / S` | **Postgres, MySQL** |
| IOPS | `provisionedIops / iopsPerRequest` | Диски, RDS |
| Сеть | `networkMbps / (payloadBytes · 8 / 10⁶)` | CDN, файлы, реплики |
| Память | `memoryGb / memoryPerRequestGb` (для in-memory) | Кэши, векторные БД |
| Партиции | `partitions · maxRpsPerPartition` | Kafka, Kinesis, DynamoDB |
| Вендорский юнит | `rcu / rcuPerRead`, `wcu / wcuPerWrite` | DynamoDB |
| Кворум | `1 / quorumRoundTripSec` | etcd, ZooKeeper |
| Лицензия/квота | `quotaRps` | Внешние API |
| Ожидание зависимостей | `instances · concurrencyPerInstance / (S + S_зав)` | Сервисы, шлюзы, воркеры |

Движок обязательно сохраняет `boundBy` — **имя ресурса, давшего минимум**. Это главный
дидактический артефакт: «упёрлись не в CPU, а в `maxConnections`».

```
ρ_i = λ_offered_i / capacity_i
```

**Автоскейлинг** (в steady-state): `instances = clamp(ceil(λ·S/(concurrency·targetUtil)), min, max)`.
В transient-режиме добавляется `scaleUpLagSec` — и видно, как во время всплеска сервис
захлёбывается, пока приезжают новые поды.

### 4.а. Ожидание зависимостей: медленный сосед занимает пул

Слот пула исполнителей занят всё время, пока запрос в работе, — включая ожидание ответа
синхронной зависимости. Поэтому у блока, объявившего пул (`concurrencyPerInstance` либо
`concurrency`), к списку ограничителей добавляется ресурс `blocking`:

```
S_зав = Σ по синхронным рёбрам e:  вызовов(e) · min(сеть(e) + S_цели + W_цели + S_зав(цель),
                                                    timeout(e))
        при callMode = parallel вместо суммы берётся самое долгое плечо

limit_blocking = instances · concurrencyPerInstance / (S + S_зав)
```

Слагаемые:

* **вызовов(e)** — сколько раз запрос ходит по ребру: доли `call.share · fanout`, уменьшенные
  на попадания в кэш и на долю маршрутизации (у балансировщика доли по всем плечам дают 1);
* **`S_цели + W_цели`** — обслуживание цели плюс её ожидание в очереди: насыщенная зависимость
  держит слот дольше, и это ровно тот каскад, ради которого ограничитель и вводится;
* **`S_зав(цель)`** — ожидание уже самой цели, так каскад доезжает по всей цепочке;
* **`timeout(e)`** — потолок плеча: дольше таймаута вызывающий поток не ждёт.

Асинхронные рёбра (`async`, `stream`, `cdc`, `batch`) и репликация пул не держат: отправитель
не ждёт ответа. Блок без объявленного пула ограничителя не получает — придумывать ему пул из
воздуха движок не станет.

Значение `S_зав` считается по предыдущему проходу итерационного решателя (§3.3) и входит в
критерий сходимости наравне с λ, поэтому длинная цепочка «сервис → сервис → БД» сходится за
несколько лишних проходов, а не застывает на полпути. Автоскейлинг сервиса тоже видит `S_зав`:
поды считаются по времени удержания слота, а не по чистому CPU.

Чего этот ограничитель **не** делает: собственное время обслуживания `S` остаётся прежним, и
формула ожидания в очереди (§5.1) по-прежнему считается от `S`, а не от `S + S_зав`. Значит,
эффект «очередь растёт, потому что каждый запрос держит слот дольше» приходит через ёмкость
и ρ, но не через дополнительную поправку к времени ожидания.

### 4.1. Потолок схемы (capacity sweep)

Ёмкость считается по узлам, а вопрос у пользователя системный: **сколько выдержит схема целиком**.
Ответ ищется бинарным поиском по множителю трафика (`ceiling.ts`):

```
насыщение(m) = max по узлам ρ_i при λ, умноженной на m   ≥ 1

m ← 1, затем удвоение вверх до первого насыщения (потолок множителя 4096)
    либо деление пополам вниз, если схема насыщена уже сейчас
затем 12 шагов деления отрезка [живой, насыщенный] пополам
```

Возвращается суммарный входящий RPS в последней живой точке, множитель к текущему трафику,
узел-ограничитель и его `boundBy` — «схема держит 47 300 rps, дальше упирается в Postgres
(соединения)». Множитель меньше единицы означает, что схема **уже** за потолком.

Поиск гоняет только решатель потоков и ядро ёмкости: Monte-Carlo, transient, пробы и находки не
считаются, поэтому весь sweep — до 30 прогонов решателя (7 мс на демо-схеме «Видеоплатформа»
из девяти узлов). Сценарий берётся тот же, что открыт в интерфейсе, поэтому «какой потолок при
отказе зоны» — такой же вопрос, как и «какой потолок вообще».

---

## 5. Шаг 4. Очереди и деградация

### 5.1. Время ожидания

Аппроксимация Сакасэгавы для M/M/c с поправкой Аллена–Каннина на вариативность (G/G/c):

```
W_q(M/M/c) = S · ρ^(√(2(c+1)) − 1) / (c · (1 − ρ))
W_q(G/G/c) = W_q(M/M/c) · (c_a² + c_s²) / 2
```

| Символ | Значение | Дефолт |
|---|---|---|
| `S` | Среднее время обслуживания | из параметров блока |
| `c` | Число обслуживающих приборов, см. ниже | |
| `c_a²` | Квадрат коэффициента вариации интервалов прихода | 1.0 (Пуассон); 2–4 при `burstiness`, 8+ при `thundering-herd` |
| `c_s²` | То же для времени обслуживания; для логнормального = `e^{σ²} − 1` | σ = 0.5 → 0.28; σ = 1.2 → 3.1 |

**Приборы считаются по связывающему ресурсу, а не по объявленному параллелизму:**

```
c = min(instances × concurrencyPerInstance, capacity · S)
```

Второе слагаемое — закон Литтла наоборот: сколько обслуживаний идёт параллельно, когда узел
работает на пределе ёмкости. Без этой поправки сервис с `concurrencyPerInstance = 200` на четырёх
ядрах считался бы M/M/800 и не показывал бы очереди даже на 95% загрузки, хотя реально держит
в обслуживании 107 запросов — и ожидание на подходе к ёмкости занижалось на порядок.

Полное время на узле: `T_i = S_i + W_q,i`. Длина очереди: `L_q = λ · W_q` (закон Литтла).

**Блок без очереди.** `queueLimit = 0` — это не «ждём ноль и всё проходит», а отказ вместо
ожидания: свободного прибора нет — запрос отбит. Доля отбитых считается формулой Эрланга B
`B(c, a)` при `a = ρ·c` приборах-эрлангах, и она отлична от нуля даже ниже насыщения: при четырёх
приборах и ρ = 0.75 теряется 14 % запросов. Выше насыщения берётся большее из блокировки и
детерминированного сброса `1 − capacity/λ`.

### 5.2. Сброс и таймауты

```
throughput_i = min(λ_offered_i, capacity_i)
p_overflow   = max(0, 1 − capacity_i / λ_offered_i)
p_timeout    = (1 − p_overflow) · exp(−timeout_i / (S_i + W_q,i))
p_fail_i     = 1 − (1 − p_overflow)(1 − p_timeout)
```

При `queueLimit = 0` (без очереди) избыток немедленно отбрасывается с 503 — «shed load».
При большой очереди latency улетает в небо, но ошибок нет: классический трейд-офф, который
симулятор показывает наглядно.

**Ожидание ограничено сливом полной очереди** — дольше, чем стоит вся очередь, ждать физически
нельзя:

```
W_drain        = queueLimit / capacity            (0, если очереди нет)
W_q            = min(W_q(G/G/c), W_drain)
queueFullShare = W_q / W_drain                    ← доля времени, что очередь стоит полной; при ρ ≥ 1 равна 1
```

`queueFullShare` уходит в свёртку задержек (§7): под перегрузкой очередь стоит полной, поэтому
ожидание перестаёт быть экспоненциальным и становится детерминированным временем слива. Именно
так насыщение попадает в p50 и p99, а не только в `errorRate`.

### 5.3. Каскад

`p_fail` узла превращается в ошибки у вызывающего, а те — в ретраи (§3.2) и в срабатывание
таймаутов вверх по стеку. Симулятор помечает **цепочку каскада** отдельным цветом: видно, как
падение одной БД валит пять сервисов.

Вторая половина каскада — ёмкостная: пока зависимость отвечает медленно, её ответ держит слот
пула вызывающего, и ёмкость вызывающего падает по §4.а. Поэтому «БД задумалась» доезжает до
сервиса дважды: ошибками и ρ.

### 5.4. Асинхронные рёбра

Async-ребро (`kind: 'async'`) **разрывает цепочку latency**: вызывающий не ждёт. Вместо этого
считается:

```
lagSeconds     = backlog / consumerThroughput
timeToDrain    = backlog / max(0, consumerThroughput − producerRps)
completionTime = enqueueLatency + lagSeconds + processingTime   ← «время до готовности результата»
```

Если `producerRps > consumerThroughput`, backlog растёт линейно; симулятор считает
`timeToRetentionOverflow = retentionBytes / (growthRate)` и выдаёт Finding «через 4.2 часа начнётся
потеря сообщений».

### 5.5. Transient-режим: ход времени

Стационарный расчёт отвечает на вопрос «выдержит ли схема такую нагрузку». Он не отвечает на
вопрос «что произойдёт в первые две минуты всплеска», потому что в стационаре автоскейлер уже
доехал, очередь уже пуста, а кэш уже прогрет. Transient-режим прогоняет схему **по шагам времени**:
на каждом шаге заново решается стационарная задача при текущем входном λ и текущем числе
инстансов, а между шагами эволюционируют величины с памятью.

Шаг длиной `Δ` считается в два прохода:

1. **проход спроса** — решатель (§3) с текущим числом инстансов возвращает `desiredInstances`
   каждого узла при нынешнем λ;
2. **лаг масштабирования** — фактическое число инстансов подтягивается к желаемому:
   `n ← n + (n_desired − n) · min(1, Δ / lag)`, где `lag = scaleUpLagSec` при росте и
   `scaleDownLagSec` при сжатии, плюс `schedulingLagSec` кластера, если узел лежит внутри
   `k8s-cluster`;
3. **проход состояния** — решатель повторяется с фактическим числом инстансов; его результат и
   становится точкой ряда.

Двухпроходность нужна, чтобы автоскейлер с нулевым лагом успевал среагировать внутри своего шага,
а не отставал на шаг из-за порядка вычислений.

Что эволюционирует между шагами:

| Величина | Как меняется |
|---|---|
| Инстансы | Первый порядок с лагом (формула выше); вниз медленнее, чем вверх |
| Очередь (backlog) | `pending = backlog + λ·Δ`, обслужено `min(pending, C·Δ)`, остаток ждёт: `backlog ← min(pending − served, L)` |
| Потерянные запросы | Всё, что не влезло в буфер: `dropped = pending − served − backlog`, `p_overflow = dropped / (λ·Δ)` |
| Прогрев кэша | `w ← w + (1 − w)·(1 − e^{−Δ/τ})`, `τ = M / λ_read` в пределах 20…900 с; hit ratio узла умножается на `w` |
| Ядовитые сообщения | `stuck ← stuck + share·λ·Δ − retire·Δ`; занятая ими доля ёмкости консьюмера — `stuck / (redelivery · C)`; при наличии `dlq` они уходят после `POISON_MAX_ATTEMPTS` попыток, без него копятся |

Размер буфера `L` зависит от узла: для синхронного это `queueLimit` (запросов), для узла за
async-ребром и для брокеров — `C · BUFFERED_BACKLOG_SEC`, то есть час ёмкости. Отсюда главное
свойство модели: **очередь не исчезает вместе с нагрузкой**. После спада она рассасывается со
скоростью `C − λ`, и время восстановления видно на ряде.

Задержка в точке ряда считается аналитически, без Monte-Carlo (иначе прогон стоил бы 60 свёрток):

```
p99 = S · e^{σ·2.3263} + min(W_q · ln(1/0.01), L/C) + backlog/C
```

— квантиль логнормального обслуживания, экспоненциальный хвост ожидания (ограниченный временем
полного слива очереди) и время разбора накопленного backlog.

Входное воздействие задаёт сценарий (§12): форма `λ(t)` — ступенька, рампа, плато или залп —
плюс окна, в которых узлы выключены (`db-failover`), замедлены (`slow-dependency`), потеряли часть
ёмкости из-за перекоса ключей (`hot-key`) или лишились бюджета ретраев (`retry-storm`).
Перекос считается через число шардов: `C_eff = C / (f·N + 1 − f)`, где `f` — доля трафика на
горячий ключ, `N` — число шардов. К λ добавляется детерминированный по seed джиттер
`exp(σ_j·ξ)`, `σ_j = 0.02 · c_a²` (не больше 0.12).

Результат прогона — ряд `timeline`: шаг, горизонт и по точке на шаг. В точке лежат вход λ,
пропускная способность, доля ошибок, пик утилизации, худший p99, суммарный backlog, суммарное
число инстансов, флаг нарушения SLO и разрез по всем узлам (λ, ёмкость, утилизация, инстансы
фактические и желаемые, очередь, ошибки, p99, hit ratio). Нарушением считается доля ошибок выше
1% или утилизация ≥ 1 хотя бы у одного узла; в сводке ряда есть момент первого нарушения и момент
восстановления.

Стационарный результат при этом никуда не девается: для transient-сценария он считается **в худшей
точке** воздействия (пик нагрузки, узел выключен, зависимость замедлена) — это и есть ответ на
вопрос «а если бы это состояние держалось вечно». Сценарий, которому не на чем сработать
(`db-failover` без хранилища, `slow-dependency` без внешней зависимости), ряда не даёт вовсе.

---

## 6. Кэш: hit ratio как производная величина

Ключевая обучающая механика: **hit ratio не задаётся, а вычисляется** из размера кэша и
распределения популярности.

Популярность ключей — Ципф с параметром α:

```
M = cacheMemoryBytes / (keyBytes + valueBytes + overheadBytes)      # сколько ключей влезло
h = H(M, α) / H(N, α),   где H(n, α) = Σ_{k=1..n} k^(−α)
```

Для α = 1 (типичный веб-трафик) при больших n: `H(n,1) ≈ ln n + 0.5772`, то есть

```
h ≈ (ln M + 0.577) / (ln N + 0.577)
```

| N (уникальных ключей) | M (влезло в кэш) | h |
|---|---|---|
| 10⁹ | 10⁶ | ≈ 0.67 |
| 10⁹ | 10⁷ | ≈ 0.78 |
| 10⁷ | 10⁶ | ≈ 0.86 |
| 10⁷ | 10⁴ | ≈ 0.58 |

Пресеты α: `0.6` — равномерный (плохо кэшируется), `1.0` — веб, `1.3` — соцсеть/новости,
`2.0` — сильно скошенный (звёзды, тренды).

**Поправки:**
* Инвалидация записью: `h_eff = h · (1 − writeShare · invalidationFactor)`.
* TTL: попадание засчитывается, только если к ключу уже обращались внутри окна TTL. Для ключа `k`
  с долей `p_k = k^(−α) / H(N, α)` обращения — пуассоновский поток интенсивности `λ_read · p_k`,
  поэтому вероятность попадания равна `1 − e^(−λ_read · p_k · TTL)`, а по всем поместившимся
  ключам:

  ```
  h(TTL) = Σ_{k ≤ M} p_k · (1 − e^(−λ_read · p_k · TTL))
  ```

  Считать миллионы слагаемых незачем: первая тысяча ключей суммируется точно, а хвост разбивается
  границей `k* = (λ_read · TTL / H(N, α))^(1/α)` — до неё ключ практически наверняка успевают
  переспросить (вклад ≈ `p_k`), после неё вероятность мала и линейна (вклад ≈ `λ_read · TTL · p_k²`).
  Итог не может превысить `H(M, α) / H(N, α)` — того, что физически помещается.

  Это важнее, чем кажется. Раньше TTL считался по равномерной модели — «средний интервал
  повторного обращения = M / λ_read» — и накладывался множителем поверх ципфовской доли. Две
  несовместимые модели давали абсурд: кэш, вмещающий все 10⁷ ключей, при λ = 694 запр./с и TTL
  300 с показывал `h ≈ 0.02` вместо честных `≈ 0.66`, и нагрузка на базу под кэшем оказывалась
  завышена в десятки раз. Горячий ключ переспрашивают сотни раз в секунду — его TTL не задевает,
  а холодный хвост и без TTL почти не даёт попаданий.

  | TTL | 1 с | 10 с | 60 с | 300 с | 1 ч | 24 ч |
  |---|---|---|---|---|---|---|
  | `h` (N = M = 10⁷, α = 1, λ = 694 запр./с) | 0.29 | 0.43 | 0.56 | 0.66 | 0.81 | 0.98 |

  Скошенность решает: при тех же условиях и TTL 300 с α = 0.6 даёт 0.11, α = 1.3 — 0.95.
* Холодный старт: `h(t) = h_∞ · (1 − e^{−t/τ})`, `τ ≈ M / λ_read` — используется в сценарии `cache-flush`.
* Hot key: доля трафика на топ-1 ключ = `1/H(N,α)`; если она превышает пропускную способность
  одного шарда — Finding `hot-key`, даже при отличном общем hit ratio.

Ручной override разрешён, но помечается бейджем «допущение пользователя» и учитывается в рубрике
заданий (нельзя «выиграть», выставив hit ratio = 0.999).

---

**Режим «Авто» и override.** Производная величина остаётся производной, пока её не переопределили:
`hitRatioMode = auto` (по умолчанию у узлов-кэшей) берёт число из формулы выше,
`hitRatioMode = manual` — из `hitRatioOverride`. Решение принимает `resolveHitRatio` в
`cacheModel.ts`, одно на все пути: и на показанный hit ratio узла, и на поглощение трафика этим
узлом. Прогрев кэша в transient-режиме и сценарий `cache-flush` действуют на оба режима — ручное
число тоже гаснет, иначе «сброшенный» кэш продолжал бы поглощать 90% чтений.

## 7. Шаг 5. Свёртка задержек: Monte-Carlo

Аналитически сложить p99 по хопам нельзя (p99 суммы ≠ сумма p99). Поэтому — прямое
семплирование по дереву вызовов, с фиксированным seed.

```
для каждого Flow f:
  для s = 1..N (N = 20 000):
     latency[s] ← walk(entryNode, f, rng)
  p50, p95, p99 ← квантили latency

walk(node, flow, rng):
  t ← sample_service_time(node, rng)          # LogNormal(median = S, σ)
  t ← t + sample_queue_wait(node, rng)        # W_q · (queueFullShare + (1 − queueFullShare) · Exp(1)), §5.2
  если replicationMode ∈ {sync, semi-sync} и rng.bernoulli(writeShare):
     t ← t + ackShare · RTT_до_самой_дальней_реплики        ← кворум ждёт реплику (§7а.3)
  если node из групп edge/clients:  ветви ← одно ребро, выбранное по потоку ветви   ← трафик делится
  иначе:                            ветви ← все sync-рёбра, каждое повторено fanout раз  ← веер
  для каждой ветви e:
     если e — кэш и rng.bernoulli(h) → пропустить (попадание, downstream не вызывается)
     t_e ← network_latency(e, rng) + walk(target(e), flow, rng)
     учесть ретраи: с вероятностью p_fail — повтор с задержкой backoff
  если node.callMode = sequential:  t ← t + Σ t_e
  если node.callMode = parallel:    t ← t + max(t_e)          ← tail amplification
  вернуть t
```

**Балансировка против веера — то же правило, что и в решателе (§15.2).** Плечи, между которыми
трафик делится (узлы групп `edge` и `clients`), складывать нельзя: запрос уходит ровно в одно из
них. Выбирается плечо **по посчитанному потоку ребра**, а не по его `weight`: гео-маршрутизация,
отказ региона и веса уже сведены решателем в λ ветви, и обход обязан идти туда же, куда ушёл
трафик. Иначе клиент из Америки, которого маршрутизатор целиком увёл в американский регион,
половину сэмплов считал бы по европейскому плечу. Веса остаются запасным вариантом на случай, когда
поток нулевой (схема без нагрузки). Плечи, которые вызываются все (веер), складываются в
`sequential` и дают максимум в `parallel`. Число вызовов на одно ребро — это `fanout` ребра:
целая часть разыгрывается как повторные вызовы, дробная — как Бернулли, поэтому `fanout = 0.5`
означает «downstream вызывается на каждом втором запросе», а `fanout = 10` стоит десяти вызовов и
в ёмкости, и в SLO.

**Веер шире шестнадцати вызовов досчитывается, а не обрезается.** Разыгрывать тысячу вызовов на
каждом сэмпле дорого, поэтому на одно ребро приходится не больше `MAX_CALLS_PER_EDGE = 16`
разыгранных вызовов. В `sequential` остаток учитывается масштабированием суммы (`calls / 16`),
а в `parallel` сумма ни при чём — там важен максимум, и он у 1000 вызовов больше, чем у 16.
Поправка берётся из асимптотики максимума логнормальной величины:

```
σ̂ ← стандартное отклонение ln(t_e) по разыгранным плечам, но не больше 2
множитель ← exp( σ̂ · ( √(2 ln N) − √(2 ln n) ) )      N — всего вызовов, n — разыграно
самое долгое плечо ← самое долгое плечо · множитель
```

Одинаковые плечи (σ̂ = 0) хвост не удлиняют — при нулевом разбросе максимум не растёт с числом
вызовов. Разброс ограничен сверху двойкой: это верхняя граница `serviceTimeSigma` в каталоге, и
она же не даёт шуму выборки раздуть хвост. Масштабируется вместе с плечом и его вклад в водопад,
поэтому сумма по хопам не расходится с итогом.

**Что это даёт бесплатно:**
* Корректный «эффект хвоста при веере» (*tail at scale*): при 10 параллельных вызовах с p99 = 50 мс
  p99 всего запроса ≈ p99 максимума ≈ значительно хуже 50 мс. Один из самых недооценённых эффектов
  в system design — и он виден на графике сразу.
* Корректный вклад ретраев и таймаутов в хвост.
* Кэш-промахи как бимодальное распределение (виден «горб» на гистограмме).
* Цену синхронной репликации: запись ждёт подтверждения реплики, и это видно в p99 записи.

**Сетевые задержки** (`network_latency`) берутся из топологии: внутри AZ / между AZ / между
регионами / интернет — см. §11.

**Крюк через океан.** Ребро из блока вне регионов (глобальный балансировщик, DNS, CDN) в блок
внутри региона получает добавку — **крюк**: насколько дальше выбранный регион, чем ближайший
доступный, для тех зон, которые в это плечо и уехали:

```
крюк(e) = Σ_zone доля(zone → e) · max( RTT_гео(zone, регион(e)) − min_r RTT_гео(zone, r), 0 )
```

Плечо с ненулевым крюком компилируется как `cross-region`, и его байты считаются по межрегиональной
цене. Смысл: маршрутизация в ближайший регион не стоит ничего сверх клиентского RTT (крюк = 0,
плечо остаётся внутризональным), а вот раздача глобальной аудитории по весам, отказ региона или
`failover` в дальний регион стоят разницы по матрице §11.1. Пример: клиент из Европы, которого
весами увели в Северную Америку, платит 80 − 25 = 55 мс сверху; он же при гео-маршрутизации не
платит ничего.

Крюк считается по зонам клиентов, достижимых до этого блока, а не по одному клиенту, поэтому у
глобальной аудитории он получается средним по зонам ветви. Отказ региона учитывается: ближайшим
считается лучший **возможный** регион, поэтому переезд в уцелевший регион виден как крюк.

---

## 7а. Модель согласованности и аномалий

Управляется настройкой **«Модель согласованности»** (решение **D2**):

| Режим | Что делает | Стоимость расчёта |
|---|---|---|
| `выкл` | Согласованность игнорируется | 0 |
| `атрибут` | Уровень согласованности — просто объявленное свойство блока; проверяется предикатами заданий | ~0 |
| **`симуляция аномалий`** (по умолчанию) | Считается частота каждого типа аномалии в операциях в секунду и в процентах, с указанием источника | O(V+E), единицы миллисекунд |

Расчёт аналитический и дешёвый — поэтому режим включён по умолчанию без ущерба для отзывчивости.

### 7а.1. Базовое распределение лага

```
L ~ LogNormal(median = replicaLagMs, σ = replicaLagSigma)
P(L > t) = 1 − Φ( (ln t − ln median) / σ )
```

| Источник лага | median по умолчанию |
|---|---|
| Синхронная репликация | ≈ 0 (но `+RTT` к latency записи) |
| Полусинхронная | 5–20 мс |
| Асинхронная внутри AZ | 20–100 мс |
| Асинхронная между AZ | 50–300 мс |
| **Между регионами** | `RTT/2 + очередь` → 30–500 мс, при перегрузке — секунды |
| CDC/Kafka-конвейер | 200 мс – 5 с |

Лаг растёт с утилизацией источника: `median_eff = median · (1 + ρ_primary²/(1−ρ_primary))` —
перегруженный primary реплицируется хуже, и это видно.

### 7а.2. Типы аномалий и формулы

**A1. Устаревшее чтение (stale read)**
```
rate_A1 = λ_read · replicaShare · E_Δt[ P(L > Δt) ]
```
При кворуме `R + W > N` → `rate_A1 ≈ 0` (в отсутствие отказов узлов).
`replicaShare` берётся из `readRouting` ребра.

**A2. Нарушение read-your-writes**
```
rate_A2 = λ_read · readAfterWriteShare · replicaShare · P(L > Δt_raw)
```
Подмножество A1, но самое болезненное для пользователя: «я отправил комментарий и не вижу его».
`Δt_raw` — распределение задержки между записью и последующим чтением из параметров Flow.

**A3. Нарушение монотонности чтений**
```
rate_A3 = rate_A1 · (1 − stickyReadShare) · (1 − 1/nReplicas) · P(|L₁ − L₂| > Δt_между_чтениями)

          где |L₁ − L₂| ≈ LogNormal(median, σ√2) — разность двух независимых лагов
```
Наблюдаемый эффект — «значение поехало назад во времени».

Формула строится **поверх ставки A1**, а не рядом с ней, и это принципиально. Нарушение
монотонности — частный случай устаревшего чтения: чтобы заметить, что время пошло назад, надо
сначала прочитать свежее значение, а потом более старое. Три множителя добавляют к A1 ровно
недостающие условия: чтение не закреплено за одной репликой (`1 − stickyReadShare`), два чтения
попали на **разные** реплики (`1 − 1/nReplicas`), и реплики разъехались сильнее, чем интервал
между чтениями.

> Ранняя редакция этого раздела задавала A3 как `λ_read · (1 − stickyShare) · … · P(|L₁ − L₂| > Δt)`
> — без единого члена, описывающего запись. Такая формула считает вероятность того, что реплики
> *разъехались*, а не того, что это *можно заметить*: на узле, где по ключу почти не пишут, она
> объявляла аномальными до 80% чтений при доле устаревших чтений 0.002%. Подмножество не может
> быть больше множества, поэтому формула исправлена, а не только реализация.

**A4. Потерянное обновление (lost update)**
```
λ_key       = λ_write / effectiveKeys            (для горячего ключа — из Ципфа, см. §6)
P_collision = 1 − e^(−λ_key · W_rmw)             (W_rmw — окно read-modify-write)
rate_A4     = λ_write · P_collision · lostFactor
```

| `concurrencyControl` | `lostFactor` | Побочный эффект |
|---|---|---|
| `none` | 1.0 | Данные теряются молча |
| `optimistic` (CAS/версии) | 0 | Коллизия превращается в ретрай: `+λ_retry`, рост latency при контенции |
| `pessimistic` (блокировки) | 0 | Коллизия превращается в ожидание блокировки: **сериализация на ключе**, которая возвращается в §4 как отдельный ограничитель ёмкости `key-lock` |
| `crdt` | 0 | Слияние без потерь, но растут метаданные |

**A5. Конфликт записи в мульти-мастере** (только `mode: active-active`, `bidirectional`)
```
T_prop      = межрегиональный лаг репликации
P_conflict  = 1 − e^(−(Σ_другие_регионы λ_key,region) · T_prop)
rate_A5     = λ_write · P_conflict
lostWrites  = rate_A5 · (conflictResolution == 'lww' ? 0.5 : 0)
```
**Last-write-wins молча теряет примерно половину конфликтующих записей** — центральный урок
заданий уровня 5. `crdt` и `single-writer-per-key` дают 0 потерь, но платят своей ценой.

**A6. Дубликат обработки**
```
rate_A6 = λ_async · [deliverySemantics == 'at-least-once'] · redeliveryRate · (1 − idempotencyKey)
redeliveryRate ≈ p_fail_consumer + rebalanceRate + visibilityTimeoutExpiry
```

**A7. Нарушение порядка**
```
rate_A7 = λ_async · [гарантия порядка отсутствует] · (1 − e^(−λ_async,key · W_processing)) · (1 − 1/lanes)

          lanes = min(partitions, consumers)     — сколько независимых дорожек у одного ключа
```
Гарантия порядка нормализуется из разнородных параметров брокеров: `orderingScope` у Kafka
(`per-key`, `per-partition`, `global` считаются упорядоченными), `queueType: fifo` у SQS. **Брокер,
у которого параметра порядка нет вообще** — RabbitMQ, NATS, Kinesis, Redis Streams, SNS —
считается неупорядоченным: между несколькими консьюмерами они порядок и не гарантируют.
Число консьюмеров берётся из `consumersPerGroup` брокера, если он есть, иначе из
`instances × concurrency` узла-потребителя.

**A8. Аномалии изоляции SQL** (грязное, неповторяющееся, фантомное чтение)
```
rate_A8 ≤ λ_read · (1 − e^(−λ_write,key · W_tx))     W_tx = serviceSec · 2
```
Множество возможных аномалий задаётся таблицей соответствия, частота — вероятностью того, что
конкурирующая транзакция затронула тот же ключ внутри окна транзакции:

| `isolationLevel` | Грязное чтение | Неповторяющееся чтение | Фантомное чтение |
|---|---|---|---|
| `read-uncommitted` | да | да | да |
| `read-committed` | — | да | да |
| `repeatable-read` | — | — | да |
| `snapshot` | — | — | — |
| `serializable` | — | — | — |

Все три кода помечены признаком `upperBound` и выводятся со знаком `≤`: различать их частоты
по отдельности значило бы выдумать точность, которой в модели нет.

### 7а.3. Средства смягчения и их цена

Каждое средство не просто «выключает» аномалию, а переносит стоимость в другое место модели —
именно этот перенос и есть предмет обучения.

| Средство | Убирает | Чем платим (считается движком) |
|---|---|---|
| `sticky-primary` после записи | A2, A3 | Рост нагрузки на primary; в мультирегионе — `+RTT` до `writeRegion` |
| `version-token` (RYW-токен) | A2 | Иногда ожидание догона реплики; сложность клиента |
| `wait-for-lag` | A1, A2 | `+E[L]` к latency чтения — прямо в p99 |
| Кворум `R + W > N` | A1 | `+latency` (ждём R ответов), падение доступности при отказах |
| Синхронная репликация | A1, A2, RPO = 0 | **`+RTT` к каждой записи** (считается, см. ниже), падение write-throughput (не считается) |
| `optimistic` / CAS | A4 | Ретраи и рост latency при контенции |
| `pessimistic` блокировки | A4 | Сериализация на ключе → новый ограничитель ёмкости |
| `crdt` | A4, A5 | Ограничения на типы данных, рост объёма метаданных |
| `single-writer-per-key` | A5 | `+RTT` для «чужих» ключей |
| `idempotencyKey` | A6 | Хранение ключей идемпотентности + их TTL и объём |
| Ключ партиционирования | A7 | Ограничение параллелизма консьюмеров |

**Что из таблицы считает движок.** Кворум `R + W > N` (`quorumN/R/W` у `mongodb`, `cassandra`,
`scylla`, `aurora`) обнуляет A1 вместе с производными от неё A2 и A3 — читатель видит хотя бы одну
свежую реплику. Липкие чтения `stickyReadShare` гасят A2 и A3 пропорционально доле. Объявленная
`consistencyModel` работает как заявка на гарантию: `read-your-writes` убирает A2, `monotonic` —
A3, а `linearizable` поверх асинхронной репликации не убирает ничего, потому что подкрепить
заявку нечем (для A1 нужна ещё и `replicationMode = sync`). A4 снимает любой `concurrencyControl`,
кроме `none`; A5 снимают `conflictResolution = crdt` и `single-writer-per-key` на политике
мультирегиона, а также `concurrencyControl = crdt` на самом хранилище. A6 снимает идемпотентность
ребра или консьюмера; на синхронном ребре с `retries > 0`, ведущем в блок с
`idempotencyRequired = true`, A6 наоборот появляется — вызов, оборвавшийся по таймауту, будет
повторён, хотя эффект уже применён:

```
доля дублей = 1 − (1 − e^(−timeout / T_ответа)) · (1 − p_fail)
```

Оценка помечена `upperBound`: не каждый оборванный вызов успел примениться на той стороне.

**Цена конкурентного доступа.** `concurrencyControl` — это выбор из трёх издержек, и движок
считает все три (`contention.ts`). Общая для них величина — окно чтения-изменения-записи
`W = 2 · S` и интенсивность записей на один ключ `λ_key = λ_write / keys`, где `keys` берётся из
параметра блока (`rowCount`, `documentCount`, `itemCount`, `uniqueKeys`, `keyCount`; при их
отсутствии — 10⁶). Вероятность того, что за окно тот же ключ тронет кто-то ещё:

```
p_collision = 1 − e^(−λ_key · W)
```

| `concurrencyControl` | Что происходит | Чем платим |
|---|---|---|
| `none` | Конкурент затирает наше обновление | Аномалия A4 с частотой `λ_write · p_collision` |
| `optimistic` | Транзакция откатывается и повторяется | Повторы: `λ_offered += λ_write · (1/(1 − p_collision) − 1)`, попыток не больше 5 |
| `pessimistic` | Записи в один ключ выстраиваются в очередь | Ограничитель ёмкости `key-serialization`: `keys / (writeShare · W)` |
| `crdt` | Расхождение сливается типом данных | A4 и A5 сняты, цена — в объёме метаданных (не считается) |

Повторы `optimistic` идут в `λ_offered` тем же путём, что и ретраи по ошибке (§3.2), поэтому
дальше сами поднимают ρ, ожидание и p99 — при узком ключевом пространстве это лавина, которую
видно на схеме находкой `contention-retries`. Ограничитель `pessimistic` работает симметрично:
он не даёт ρ вырасти, а вместо этого объявляет потолок ёмкости, привязанный к числу ключей, —
`boundBy` узла становится «сериализацию на ключе», и решение задачи упирается не в железо,
а в схему данных.

**Чего движок пока не берёт с этих средств** — рост нагрузки на primary от липких чтений,
`+RTT` за «чужие» ключи при `single-writer-per-key`, `wait-for-lag` как надбавку к задержке
чтения. Перечень — в `docs/06-backlog.md`.

**Цена подтверждения репликации.** `replicationMode` хранилища добавляет к каждой записи ожидание
подтверждения реплик — иначе `sync` бесплатно обнулял бы устаревшие чтения, read-your-writes и RPO:

```
ackSec = ackShare · RTT_реплики
ackShare      = 1 для sync (ждём самую медленную реплику), 0.5 для semi-sync (ждём самую быструю)
RTT_реплики   = networkMs самого дальнего нарисованного ребра репликации,
                иначе RTT между AZ (0.35–1.2 мс), если у блока объявлены реплики
```

В Monte-Carlo надбавка разыгрывается по Бернулли с вероятностью `writeShare` узла, поэтому попадает
только в записи. Межрегиональная синхронная репликация стоит полного RTT (для Европа ↔ Америка это
80 мс на запись) — ровно та цена, которую в реальности платят за RPO = 0.

### 7а.4. Что видит пользователь

```
Согласованность · поток «checkout»
  Устаревшие чтения           2 340 op/s   0.42% чтений   ← Postgres replica (лаг p99 180 мс)
  из них read-your-writes     2 080 op/s   0.37%          ← самое заметное для пользователя
  Потерянные обновления          12 op/s   0.9% записей   ← нет CAS на строке `balance`
  Конфликты мульти-мастера        4 op/s   0.3% записей   ← LWW теряет ≈ 2 записи/с
  Дубликаты обработки             0 op/s                  ← ключ идемпотентности включён ✓

  [Показать источники на схеме]   [Как это починить]   [Почему так посчитано]
```

Реализовано так: строка аномалии раскрывается формулой и подставленными значениями (то самое
«почему так посчитано»), имена узлов — кнопка, подводящая камеру к источнику. Под таблицей идёт
список включённых средств смягчения: что включено, на каком узле и какие аномалии это снимает.
Список считает движок (`ConsistencyResult.mitigations`), а не панель, поэтому разойтись с расчётом
он не может; при выключенной модели аномалий он пуст.

---

## 8. Шаг 6. Хранилище, память, логи

### 8.1. Полезные данные

```
growthGbDay = λ_write · 86400 · recordBytes · (1 + indexOverhead) · RF / compressionRatio / 10⁹
totalGb(t)  = initialGb + growthGbDay · t          (с учётом retention: t = min(t, retentionDays))
```

Дефолты: `indexOverhead` 0.2–1.0 (зависит от числа индексов), `RF` 1 (одна нода) / 3 (Cassandra,
HDFS) / 2–6 (реплики SQL), `compressionRatio` 1 (SQL по умолчанию) / 5–15 (ClickHouse, Parquet).

### 8.2. Медиа и блобы

```
storageGbDay = uploadsPerDay · (sourceSizeGb + Σ renditions_i.bitrateMbps · durationSec / 8 / 1000)
egressGbDay  = viewsPerDay · watchedSec · bitrateMbps / 8 / 1000
```

### 8.3. Логи, метрики, трейсы

```
logsGbDay    = Σ_services λ_i · linesPerRequest_i · bytesPerLine_i · 86400 · samplingRate / 10⁹
metricsGbDay = activeSeries · (86400 / scrapeIntervalSec) · bytesPerSample / 10⁹
tracesGbDay  = Σ λ_i · traceSampling · spansPerRequest · spanBytes · 86400 / 10⁹
```

`spansPerRequest` берётся из глубины дерева вызовов автоматически — то есть усложнение
архитектуры автоматически удорожает наблюдаемость. Полезный урок.

### 8.4. Память

```
cacheMemoryNeededGb = uniqueActiveKeys · (keyBytes + valueBytes + overheadBytes) / 10⁹
dbWorkingSetGb      = hotRowsFraction · rowCount · rowBytes / 10⁹
wsConnectionsMemGb  = concurrentConnections · memoryPerConnKb / 10⁶
```

### 8.5. Бэкапы

```
backupGb = totalGb · (fullsPerMonth + incrementalRatio · incrementalsPerMonth) · retentionMonths
```

Политика одна на всю модель (`BACKUP_POLICY` в `constants.ts`): один полный снимок в месяц плюс
29 суточных инкрементов по 3 % от объёма при месячном хранении — **1.87 объёма основных данных**.
Бэкапы начисляются блокам групп `sql` и `nosql` (`BACKUP_GROUPS`): это данные, потеря которых
невосстановима, в отличие от кэша, индекса поиска или витрины OLAP, которые перестраиваются.

Объём попадает в `NodeResult.backupGb` и в `Totals.backupGb`, показывается в доке рядом с
хранилищем. **В счёт он не входит** — как и логи: это строка хранилища, а не стоимости, и §9 не
знает про бэкапы. Отдельная плата за них сдвинула бы экономику всех заданий, а бюджеты каталога
выставлены по модели без них; см. `docs/06-backlog.md`.

### 8.6. Ключи идемпотентности

```
idempotencyGb = λ_ключей · ttlHours · 3600 · bytesPerKey / 10⁹
```

Политика одна на всю модель (`IDEMPOTENCY_POLICY` в `constants.ts`): TTL 24 часа, 256 байт на
запись — ключ, отпечаток запроса, статус и ссылка на результат. При 1 000 запросов в секунду это
22 ГБ, которые кто-то должен хранить и чистить; на схеме такой блок обычно не нарисован, поэтому
объём и показывается отдельной строкой.

Ключи начисляются двум ролям, и обе выводятся из графа, а не из флажка на узле:

| Кто платит | Когда | `λ_ключей` |
|---|---|---|
| Вызывающий блок | Синхронное ребро ведёт в блок с `idempotencyRequired = true` (`external-api`, `payment-external`) | Поток записей на ребре |
| Консьюмер | Асинхронное ребро помечено `idempotent` или консьюмер объявил `idempotent = true` | Весь поток сообщений на ребре |

Провайдеру ключи не начисляются: хранилище дедупликации живёт на нашей стороне. Объём попадает в
`NodeResult.idempotencyGb` и `Totals.idempotencyGb` и, как бэкапы и логи, в счёт не входит.

---

## 9. Шаг 6б. Стоимость

```
costMonth_i = compute + storage + egress + requests + managedPremium
   compute  = instances · costPerInstanceHour · 730
   storage  = totalGb · costPerGbMonth  (+ IOPS-плата для io2/provisioned)
   egress   = egressGbMonth · costPerGbEgress
   requests = (λ · 2.63·10⁶ / 10⁶) · costPerMillionRequests      # 2.63e6 сек в месяце
   managed  = коэффициент 1.0–2.5× к self-hosted
```

Профили цен (`pricingProfile`): `aws-2026-q2`, `gcp-2026-q2`, `hetzner-2026-q2`, `on-prem` —
каждый с полем `asOf`. Профиль задаёт ставки egress, межзонного и межрегионального трафика,
цену миллиона запросов и `managedMultiplier`.

**`managedMultiplier` — не наценка за managed поверх self-hosted, а уровень цен на управляемые
сервисы в этом профиле.** Собственные цены блока (`costPerInstanceHour`, `costPerGbMonth`) заданы
по прейскуранту большого облака, поэтому у `aws-2026-q2` множитель равен 1. У блоков с флагом
`managed` (`cdn`, `dns`, `glb`, `s3`, `dynamodb`, `sqs`, `serverless` и другие — всего 25) на него
умножаются `compute`, `storage` и `requests`; `network` не умножается, он и так считается по
ставкам профиля. Блоки, которые держат сами, множителем не затрагиваются: их железо стоит одинаково
у любого провайдера, а разницу в цене администрирования модель не изображает.

**Egress платится там, где трафик реально покидает периметр** — иначе экономика CDN держалась бы
на бесплатном наполнении кэша. Три правила атрибуции:

| Что за трафик | Кто платит | За что |
|---|---|---|
| Ребро от клиента (`scope = internet`) | тот, кто отвечает клиенту | `responseBytes` ответа |
| Ребро от CDN к origin (PoP стоят вне периметра) | origin | `responseBytes` наполнения кэша |
| Вызов внешнего сервиса (`external-api`) | вызывающий | `requestBytes` запроса |

Трафик между AZ и между регионами оплачивается отдельно, по ставкам `crossAzPerGb` и
`crossRegionPerGb` профиля цен.

**Egress — главный источник «вау»:** прямая раздача видео из S3 при $0.09/ГБ против CDN c
$0.02/ГБ и кэш-хитом 95% (то есть 5% ходят в origin и оплачиваются как egress origin) даёт
$0.09 против $0.02 + 0.05·$0.09 = $0.0245 за гигабайт — разница в 3.7×. Кэш-хит здесь виден в счёте
напрямую: каждый процент промахов добавляет к счёту 1% трафика по цене origin, поэтому CDN с
хитом 0.92 вместо 0.98 стоит заметно дороже.

---

## 10. Доступность и отказоустойчивость

```
Последовательный участок:   A = Π A_i
Резервированная группа n из k:  A = Σ_{j=k..n} C(n,j) · A_i^j · (1−A_i)^(n−j)
Учёт failover:              A_eff = A − (failuresPerYear · failoverSec) / 31 536 000
Доступность Flow:           A_flow = Π по критическому пути
Error budget:               (1 − A_flow) · 43 200 мин/мес
```

**Сколько реплик нужно живыми** (`k`) блок объявляет сам — необязательный хук `quorum(params)` в
`ComponentModel`. По умолчанию `k = 1`: достаточно одной живой реплики, и формула вырождается в
`A = 1 − (1−A_i)^n`. Кворум объявили те, для кого он физика, а не настройка: `mongodb`, `cassandra`,
`scylla` и `aurora` берут `k = quorumW`, `etcd` — большинство узлов `⌊n/2⌋ + 1`. Разница
дидактическая: пять узлов etcd при `A_i = 99.9 %` дают не «пять девяток от резерва», а
меньше — большинство должно быть живым, и лишний узел помогает слабее, чем кажется.

К независимому отказу примешивается коррелированный: `unavailable = группа · 0.95 + одиночный · 0.05`.
Пять процентов отказов бьют по всем репликам сразу (общий коммутатор, общий баг, общий деплой),
и без этой примеси резервирование давало бы бесконечные девятки.

**Цена переключения.** Резерв не бесплатен: пока идёт failover, трафик не обслуживается. Штраф
`failuresPerYear · failoverSec / 31 536 000` вычитается только у блоков с резервом (`n > 1`) —
одиночный блок ничего не переключает, его простой уже сидит в собственной `availability`.
`failuresPerYear = 2` — константа движка, `failoverSec` берётся с блока (по умолчанию 30 с).
Отсюда неочевидный вывод: ручное переключение за десять минут упирает схему в четыре девятки
независимо от числа реплик.

**Последовательное против резервированного.** Доступность потока считается обходом графа от
клиента: у балансирующих блоков (группа `edge` — `lb-l4`, `lb-l7`, `glb`, `dns`, `api-gateway`)
несколько исходящих ветвей означают **альтернативы** — `A = 1 − Π(1 − A_ветви)`, health-check
выводит павшего из ротации. У остальных блоков несколько исходящих ветвей означают **зависимости**:
сервис, которому нужны и база, и кэш, перемножает их. Раньше перемножались все достижимые узлы,
и две реплики сервиса за балансировщиком ухудшали доступность вместо того, чтобы её улучшать.

**SPOF-детектор:** для каждого узла критического пути проверяется, есть ли альтернативный путь при
его удалении (по сути — поиск точек сочленения / мостов в графе Flow). Узел без резерва и с
`instances = 1` или с `azSpread = 1` — Finding `spof`. Блоки с флагом `managed` в определении
(`cdn`, `dns`, `glb`, `s3`, `dynamodb`, `sqs`, `serverless`) из проверки исключены: избыточность
внутри такого сервиса держит провайдер, а счётчика инстансов у него нет. Тот же флаг убирает их
из антипаттерна `over-provisioned` и из оси рубрики «Запас прочности» — за простой usage-priced
сервиса никто не платит.

**Домены отказа:** узлы внутри одной группы `az` падают вместе в сценарии `az-failure`.

**Зона без блока `az`.** Рисовать зоны явно нужно не всем схемам, поэтому сценарий умеет работать и
без блока: инстансы узла считаются размазанными по `azSpread` зонам (по умолчанию — три, но не
больше числа инстансов), и потеря одной зоны снимает `1/spread` мощности:

```
доля_выжившего(узел) = (spread − 1) / spread,   spread = min(azSpread, число инстансов)
```

* `multiAz = true` (управляемая БД со standby) — переживает зону целиком, доля 1;
* один инстанс — это одна зона: узел **выключается** (тот самый SPOF, ради которого сценарий и
  нужен);
* блок без объявленных инстансов (региональный управляемый сервис — очередь, объектное хранилище)
  зоной не задет.

Явный блок `az` на схеме по-прежнему главнее: он есть — гасится он.

---

## 11. Константы и дефолты

Все значения вынесены в датасет `data/defaults/*.json` с полем `source` и `asOf`.

### 11.1. Задержки (порядки величин, 2026)

| Операция | p50 | Комментарий |
|---|---|---|
| Чтение из L1/L2 кэша CPU | ~1 нс / ~4 нс | в модели не участвует |
| Обращение к RAM | 100 нс | |
| Сжатие 1 КБ | ~2 мкс | |
| Случайное чтение NVMe SSD | 50–150 мкс | |
| Случайное чтение SATA SSD | 100–500 мкс | |
| Seek HDD | 5–10 мс | |
| RTT внутри AZ | 0.2–0.5 мс | |
| RTT между AZ | 0.5–2 мс | + $0.01–0.02/ГБ |
| RTT внутри континента (регион↔регион) | 10–40 мс | |
| RTT трансатлантический | 70–90 мс | |
| RTT US↔Азия | 120–180 мс | |
| TLS handshake | 1–2 RTT | +полный RTT на TCP |
| Мобильный интернет 4G / 5G | 40–70 / 10–30 мс | |
| Redis GET (по сети) | 0.3–1 мс | |
| Postgres point-select (в buffer pool) | 0.3–1 мс | |
| Postgres point-select (промах, с диска) | 3–10 мс | |
| Postgres INSERT с fsync | 2–8 мс | |
| MongoDB find по индексу | 0.5–2 мс | |
| Cassandra чтение QUORUM (RF=3) | 2–8 мс | |
| DynamoDB GetItem | 3–10 мс | |
| Elasticsearch поисковый запрос | 10–100 мс | |
| ClickHouse аналитический запрос | 50 мс – 10 с | зависит от объёма скана |
| Kafka produce (acks=all) | 2–10 мс | |
| S3 GET time-to-first-byte | 20–60 мс | |
| S3 PUT (1 МБ) | 30–100 мс | |
| CDN edge hit | 10–30 мс | |
| Вызов внешнего платёжного API | 200 мс / p99 3 с | |
| Cold start Lambda (JVM / Node / Go) | 1–5 с / 200–600 мс / 100–300 мс | |

### 11.2. Дефолтные ёмкости

| Компонент | Значение по умолчанию |
|---|---|
| Сервис (Go, 4 vCPU) | S = 5 мс, concurrency = 256 → ~50k rps/инстанс (CPU-bound: ~5–15k) |
| Сервис (JVM, 4 vCPU) | S = 8 мс, concurrency = 200 |
| nginx / Envoy L7 | 20–50k rps на ядро без TLS, 5–15k с TLS |
| Postgres (8 vCPU) | 200 connections, ~10–20k point-read rps с пулером, 2–5k write rps |
| Redis (1 ядро) | 80–150k ops/s, ~10 ГБ на шард комфортно |
| Kafka (брокер) | 100–500 МБ/с, партиция ~10–50 МБ/с |
| Cassandra (нода) | 10–30k write/s, 5–15k read/s |
| Elasticsearch (нода) | 1–5k запросов/с (простые), 20–50 МБ/с индексации |
| S3 | 3 500 PUT / 5 500 GET на префикс |
| DynamoDB партиция | 1 000 WCU / 3 000 RCU |
| WebSocket-шлюз | 50–200k соединений на инстанс, ~10 КБ памяти на соединение |

### 11.3. Дефолтные цены (AWS us-east-1, 2026-Q2, справочно)

| Статья | Цена |
|---|---|
| vCPU-час (on-demand, general purpose) | ~$0.04 |
| ГБ RAM-час | ~$0.005 |
| EBS gp3 | $0.08/ГБ-мес + IOPS сверх 3000 |
| S3 Standard | $0.023/ГБ-мес |
| S3 Glacier Deep Archive | $0.001/ГБ-мес |
| Egress в интернет | $0.09/ГБ (первые ТБ) |
| CDN egress | $0.02–0.085/ГБ по регионам |
| Между AZ | $0.01/ГБ в каждую сторону |
| S3 запросы | $0.005/1000 PUT, $0.0004/1000 GET |
| DynamoDB on-demand | $1.25/млн WCU, $0.25/млн RCU |
| Lambda | $0.20/млн вызовов + $0.0000166667/ГБ-с |
| Managed-премия (RDS/MSK/ElastiCache) | ×1.3–2.0 к self-hosted |

---

## 12. Сценарии (Scenario Battery)

Каждый сценарий — трансформация входных условий + критерий успеха.

| ID | Что делает | Что проверяет |
|---|---|---|
| `baseline` | Номинальный трафик | Базовое соответствие SLO |
| `peak` | ×`peakFactor` (по умолчанию ×3) | Запас по мощности |
| `spike` | Ступенька ×5 на 60 с, `c_a²` = 8 | Реакция автоскейлинга, поведение очередей |
| `growth` | ×10 трафика и ×10 данных (горизонт 2 года) | Масштабируемость архитектуры, а не железа |
| `az-failure` | Выключение всех узлов одной AZ; если блока `az` на схеме нет — снятие доли мощности каждого узла по числу зон | Резервирование, отсутствие SPOF |
| `region-failure` | Выключение региона | Мультирегион, RPO/RTO |
| `cache-flush` | `hitRatio = 0`, восстановление по `τ` | Выдержит ли БД холодный кэш |
| `db-failover` | Primary недоступен `failoverSec` | Потеря записей, поведение приложения |
| `hot-key` | 40% трафика на один ключ/партицию | Шардирование, локальный кэш, репликация горячего |
| `slow-dependency` | Внешняя зависимость: p99 = 5 с | Таймауты, брейкеры, изоляция пулов (bulkhead) |
| `thundering-herd` | Одновременная инвалидация + всплеск | Single-flight, jitter, stale-while-revalidate |
| `retry-storm` | Ретраи без бюджета при деградации | Retry budget, exponential backoff, брейкер |
| `poison-message` | 1% сообщений всегда падают | DLQ, ограничение попыток |
| `stale-read` | Лаг репликации ×10 (перегруженный primary, забитый межрегиональный линк) | Read-your-writes, кворумы, sticky-маршрутизация |
| `write-conflict` | Всплеск записи в один ключ одновременно из двух регионов | Разрешение конфликтов, single-writer-per-key, CRDT |
| `lost-update` | Рост конкуренции на горячей строке в 20 раз | CAS, оптимистичные блокировки |
| `duplicate-processing` | Перебалансировка консьюмеров + повторная доставка 5% сообщений | Идемпотентность, дедупликация |
| `split-brain` | Разрыв связи на 120 с: обе стороны принимают записи, реплики расходятся | Кворум `R + W > N`, стратегия слияния, цена LWW при восстановлении |
| `black-friday` | ×20 на 4 часа + рост payload | Комбинированный стресс |

Обязательные в MVP: `baseline`, `peak`, `az-failure`, **`region-failure`**, `cache-flush`,
**`stale-read`**, **`write-conflict`**. Сценарии `stale-read` / `write-conflict` / `lost-update` /
`duplicate-processing` / `split-brain` активны только при включённой модели аномалий (§7а).

Каждый сценарий возвращает: прошёл/не прошёл (по SLO задания), деградировавшие узлы, время
восстановления, потерянные запросы/сообщения.

---

## 13. Детерминизм, производительность, честность

| Требование | Реализация |
|---|---|
| Детерминизм | Единый seeded PRNG (`xoshiro128**`), seed = хэш(схема + сценарий + версия модели). Без `Math.random()` |
| Инкрементальность | Пересчитывается только затронутый подграф + всё ниже по потоку; кэш результатов по хэшу подграфа |
| Не блокировать UI | Расчёт в Web Worker; устаревшие задачи отменяются (`AbortSignal`) |
| Live-preview | N сэмплов = 4 000 при перетаскивании ползунка, 20 000 — при отпускании |
| Версионирование модели | `modelVersion` пишется в результат; при изменении формул старые «звёзды» пересчитываются |

---

## 14. Как мы проверяем, что модель не врёт

Эта секция — ответ на риск **R1** из PRD.

1. **Золотые схемы.** 12 эталонных схем с вручную посчитанными ожидаемыми метриками; снапшот-тесты
   в Vitest. Любое изменение формул ломает снапшот и требует осознанного обновления.
2. **Проверки размерности.** Автотест: все формулы согласованы по единицам (rps × байт = байт/с).
3. **Проверки предельных случаев.** ρ→0 ⇒ W_q→0; ρ→1 ⇒ W_q→∞; c→∞ ⇒ W_q→0; hit ratio при M≥N ⇒ 1.
4. **Сверка с теорией очередей.** Для M/M/1 численный результат сверяется с точной формулой
   `W_q = ρ·S/(1−ρ)` (расхождение аппроксимации < 5%).
5. **Сверка с публичными бенчмарками.** Дефолты по Postgres/Redis/Kafka сверяются с публичными
   замерами; в датасете хранится ссылка и дата.
6. **Явные допущения в UI.** У каждой сложной метрики — иконка ⓘ с формулой, подставленными
   числами и списком допущений. Пользователь всегда видит, что это оценка, а не измерение.
7. **Дисклеймер уровня продукта:** «Это обучающая модель. Она правильно передаёт *порядки величин* и
   *относительные эффекты* архитектурных решений, но не заменяет нагрузочное тестирование».

---

## 15. Что из этой модели реализовано к концу фазы 3

Секция описывает фактическое состояние кода в `src/engine/sim/`, включая расхождения с планом выше.
Стационарный срез пришёл из фазы 1; transient-режим (§5.5), десять сценариев с ходом времени,
показания проб и водопад задержки добавлены в фазе 3. Секция обновляется вместе с движком:
расхождение, которого здесь нет, считается ошибкой.

### 15.1. Реализовано

| Шаг | Модуль | Состояние |
|---|---|---|
| [1] Компиляция топологии | `compile.ts` | Валидация, SCC (Тарьян, итеративный), порядок обхода по конденсации, размещение узлов по Region/AZ/VPC/кластеру, сетевые задержки по области (`same-az` / `cross-az` / `cross-region` / `internet`), матрица RTT между гео-зонами, надбавка `peeringLatencyMs` на межVPC-хопе и на выходе через NAT |
| [2] Решатель потоков | `solver.ts` | Демпфированные итерации (ω = 0.5, до 50 проходов, порог 0.001 по λ и по ожиданию зависимостей), усиление ретраями, поглощение кэшем, гео-маршрутизация, автоскейлинг |
| [3] Ядро ёмкости | `resources.ts` + `model` каждого блока | Декларативные ограничители, `boundBy` с формулой и подставленными значениями, ограничитель `blocking` от ожидания синхронных зависимостей (§4.а), ограничитель `key-serialization` при `concurrencyControl = pessimistic` (§7а.3) |
| [4] Очереди | `queueing.ts` | Сакасэгава + поправка Аллена–Каннина по приборам связывающего ресурса (§5.1), потолок ожидания по сливу очереди и `queueFullShare` (§5.2), сброс нагрузки, таймауты, `p_fail` |
| [5] Свёртка задержек | `latency.ts` | Monte-Carlo по дереву вызовов, балансировка по `weight` против веера по `fanout` (§7), `sequential` / `parallel`, ожидание с учётом насыщения, ожидание подтверждения синхронной репликации, ретраи с backoff, таймауты, промахи кэша |
| [5а] Согласованность | `consistency.ts`, `contention.ts` | Все восемь аномалий §7а.2: A1, A2, A3, A4, A5, A6, A7, A8. A3 строится поверх ставки A1 (иначе подмножество оказывается больше множества), A8 выводится тремя кодами с признаком `upperBound`. A6 считается и на синхронном ребре с ретраями в блок с `idempotencyRequired`. Отчёт `mitigations`: что включено, где и какие коды снимает. Цена `optimistic` и `pessimistic` (§7а.3). Разрыв связи: расхождение реплик `divergent-replicas` и конфликты слияния за время разрыва |
| [6] Производные | `derived.ts`, `cost.ts`, `availability.ts` | Хранилище, логи, объём бэкапов у `sql` и `nosql` (§8.5), объём ключей идемпотентности у вызывающих и консьюмеров (§8.6), egress по трём правилам атрибуции (§9), премия за управляемый сервис по профилю цен (§9), стоимость по профилям цен, стоимость контейнеров (ноды и control plane кластера, обработка ГБ на NAT), доступность: биномиальная группа «k из n» с кворумом от блока, коррелированные отказы, штраф за время failover, обход графа с альтернативами за балансировщиками и зависимостями у остальных, SPOF (§10) |

**NAT считается в двух местах, и они не пересекаются.** Контейнер `vpc` берёт на себя пересечение
границы: надбавку `peeringLatencyMs` на рёбрах с `viaNat` в `compile.ts` и обработанные гигабайты
по тем же рёбрам в `cost.ts`. Блок `nat-egress` берёт на себя сам шлюз: собственное время
пересылки (десятые доли миллисекунды, без повторной надбавки за границу) и трафик, реально
покидающий периметр, — `egressGbMonth` из `derived.ts`, а не проходящий объём. То есть трафик,
который пересёк границу VPC, но остался внутри, оплачивается контейнером и не оплачивается блоком,
а вышедший наружу — наоборот. Единственный случай двойного счёта — вырожденный: если нарисовать
шлюз на границе и при этом оставить `vpc.natRequired = true`, объявлены два NAT на одну границу.
Блок сам флаг не включает.
| Гео-маршрутизация | `routing.ts` | Политики `latency`, `geo`, `failover` на `dns` и `glb` (§3.4): зона клиента → регион, раздельные доли чтений и записей, обход выключенного региона. `weighted` и `simple` оставляют веса рёбер |
| Мультирегион | `multiRegion.ts` | Режимы `single`, `active-passive`, `active-active`, `read-local-write-global` (§3.4), доли трафика по регионам (сумма посчитанного трафика узлов региона к общему — не веса рёбер: маршрут выбирает `routing.ts`), трафик и стоимость репликации, RPO/RTO против целевых |
| Потолок схемы | `ceiling.ts` | Бинарный поиск по множителю трафика (§4.1): суммарный RPS насыщения, множитель к текущему трафику, узел-ограничитель и его `boundBy`. Считается по кнопке в доке, в том же сценарии, что открыт |
| [7] RuleEngine | `findings.ts` | 16 правил + аномалии как находки: `retry-storm`, `unmodelled`, `overloaded`, `saturated`, `retry-amplification`, `contention-retries`, `hot-key`, `spof`, `k8s-pods-exceeded`, `k8s-nodes-scaled`, `nat-saturated`, `client-direct-to-store`, `retry-without-idempotency`, `backlog-growing`, `read-heavy-without-cache`, `egress-dominates` |
| Потолок подов кластера | `clusters.ts` | `autoscaleNodes = false` — сумма подов режется по `nodes × podsPerNode` долями от желаемого с полом в один под на узел, зажатие применяется **вне** неподвижной точки отдельным вторым проходом решателя (иначе «зажали → ρ вырос → автоскейлер просит ещё» расходится); `autoscaleNodes = true` — кластер доращивает ноды до `ceil(requested / podsPerNode)` и платит за них. Стационар и `timeline` считают потолок одинаково |
| [8] Transient | `transient.ts` | Прогон по шагам времени (§5.5): лаг автоскейлинга, очереди с памятью, прогрев кэша, ядовитые сообщения; ряд `timeline` в результате |
| Сценарии | `scenarios.ts` | Стационарные `baseline`, `peak`, `az-failure`, `region-failure`, `stale-read`, `write-conflict`, `split-brain`; transient `spike`, `growth`, `black-friday`, `db-failover`, `cache-flush`, `hot-key`, `slow-dependency`, `thundering-herd`, `retry-storm`, `poison-message` |
| Показания проб | `probes.ts` | Все 11 типов проб (§14 в `docs/01-components.md`) считают значение, единицу и `explain` из результата симуляции; статус `ok` / `warn` / `breach` либо `no-data` с причиной (`unattached`, `unknown-target`, `unsupported-target`, `no-traffic`, `no-flow`) |
| Графика проб | `latency.ts`, `probes.ts` | `probe-latency` — гистограмма: `rollUpLatency` бакетизирует посэмпловые массивы, которые раньше выбрасывал, суммируя серии тех call site, чья цель — измеряемый узел (время одного запроса *на этом узле*, даже если он вызван несколько раз); шкала `linear` / `log`, отметки p50/p95/p99. `probe-rps` и `probe-utilization` — ряд из `timeline`, без изменений в движке. `probe-heatmap` — проекция на схему: проба отдаёт значения по узлам области, канвас берёт из них цвет рамки, полоса утилизации узла при этом остаётся своей |
| Водопад задержки | `latency.ts` | `FlowWaterfall` на каждый поток: вклад каждого хопа в p50/p95/p99 с разбивкой на обслуживание, ожидание, сеть и ретраи, доля запросов и число вызовов на хоп, покрытая хопами часть суммы против квантиля потока |
| Детерминизм | `rng.ts` | `xoshiro128**`, seed = хэш(схема + сценарий + версия модели) ⊕ `settings.seed` |

### 15.2. Расхождения с планом — принятые решения

**Flow — это клиентский блок.** Отдельной сущности Flow нет: каждый блок группы `clients`
задаёт один поток, `λ = dau · sessionsPerUserDay · requestsPerSession / 86400`, смесь операций —
из `readWriteMix`. Несколько именованных сценариев поведения на одном клиенте и покомпонентное
распределение `clientGeo` (§3.4) не реализованы; гео берётся из параметра региона и клиента целиком.

**Ветвление против веера.** Узлы групп `edge` и `clients` делят исходящий трафик между рёбрами
пропорционально `weight` (сумма долей = 1) — это балансировка. Все остальные узлы трактуют каждое
исходящее ребро как отдельный вызов на запрос (`fanout`). Это правило заменяет ручное задание
`share_e` из §3.1. **Свёртка задержек следует тому же правилу**: у балансирующего узла сэмпл идёт
ровно в одно плечо, выбранное по весам, а не суммирует все; у остальных узлов ребро повторяется
`fanout` раз. Пока обход суммировал все плечи, мульти-CDN и мультирегион завышали p99
пропорционально числу плеч, а `fanout` не стоил ничего, кроме ёмкости.

**Поглощение кэшем выводится из графа и касается только чтений.** Если узел вызывает и кэш, и
хранилище, у рёбер к хранилищам (`sql`, `nosql`, `search`, `olap`, `storage`) на `1 − h`
умножаются вызовы операций `read`, `scan`, `consume`, где `h` — hit ratio кэша-соседа; `write`,
`delete`, `publish`, `transfer` доходят до хранилища целиком. Отдельно узел с собственным параметром `cacheHitRatio` (CDN) или
`resolverCacheHitRatio` (DNS) поглощает эту долю всех своих исходящих вызовов. В Monte-Carlo то же
самое разыгрывается по Бернулли, поэтому промах виден как второй горб на распределении задержки.

**Выключенный узел — это отказ, а не экономия.** Узел, погашенный сценарием, отдаёт нулевую
пропускную способность, а весь пришедший в него трафик становится ошибкой (`errorRate = 1`) и
находкой `node-down`. Вызов в него стоит вызывающему таймаута ребра (и повторов, если они
настроены), поэтому задержка от отказа только растёт. Счёт за инстансы продолжает идти — узел
выключен, а не удалён, — а накопленные данные считаются по неповреждённому прогону: сто секунд
недоступности не стирают гигабайты. Утилизация выключенного узла — ноль: он ничего не обслуживает,
и требование «не выше 75%» не должно ломаться о мёртвый узел.

**Отказ уводит трафик к живым, а не в никуда.** Балансирующие узлы (`edge`, `clients`) делят
исходящий трафик по весам только между целями, которые сценарий не выключил, и перенормируют
доли; если под отказ попал сам клиент, его λ раскладывается по выжившим клиентам пропорционально
их нагрузке. Иначе `region-failure` не нагружал бы выживший регион, а вдвое разгружал бы схему —
и отказ выглядел бы полезнее baseline. Выключенный узел при этом продолжает показывать пришедший
к нему поток при нулевой ёмкости: в transient-ряде это его доля ошибок, а не тишина.

**Полезная нагрузка живёт на ребре.** `avgRequestKb` / `avgResponseKb` клиента подставляются в
профили вызова в момент создания ребра (`applySourcePayload`), дальше по цепочке payload не
наследуется — его задаёт каждое ребро само. Иначе один параметр клиента описывал бы и видео-чанк,
и JSON-ответ API.

**`originLatencyMs` у CDN — это задержка ребра к origin**, а не слагаемое во времени обслуживания
CDN. Так нарисованный origin не считается дважды.

**A1 переформулирована под доступные параметры.** Вместо `E_Δt[P(L > Δt)]` считается вероятность
того, что в тот же ключ была запись за время, сравнимое со средним лагом:
`P = 1 − e^(−λ_write,key · E[L])`, где `λ_write,key = λ_write / effectiveKeys`, а `effectiveKeys`
берётся из `rowCount` / `uniqueKeys` / `documentCount` / `itemCount` блока.

**Transient-сценарии заданы амплитудами, а не абсолютами.** `slow-dependency` умножает время
обслуживания внешней зависимости на 25 вместо «p99 = 5 с» из §12: у зависимостей разная база, и
множитель переносится между схемами, а абсолютная цифра — нет. `growth` растит трафик ×10 за два
года, а обещанный ×10 объём данных получается сам — из выросшего потока записей в §8.
`black-friday` — плато ×20 на 4 часа с трапецией разгона по 15 минут и ×1.5 к payload. Множитель
времени обслуживания при этом делит ёмкость узла (`C_eff = C·scale_capacity / scale_service`), что
верно для ограничителей по числу приборов и завышает ёмкость там, где узел упирается в полосу.

**Worst-moment вместо среднего.** Стационарный срез transient-сценария считается в худшей точке
воздействия, а не в средней по горизонту: иначе `db-failover` со сорокасекундным окном на
пятиминутном горизонте выглядел бы как «почти baseline», и все предикаты заданий проходили бы.

**Константы, введённые движком** (их нет в параметрах блоков, они задокументированы здесь):

| Константа | Значение | Где | Смысл |
|---|---|---|---|
| `READ_AFTER_WRITE_SHARE` | 0.5 | `consistency.ts` | Доля записей, за которыми следует чтение тех же данных |
| `READ_AFTER_WRITE_GAP_SEC` | 1 с | `consistency.ts` | Типичная пауза между записью и чтением |
| `REBALANCE_REDELIVERY_RATE` | 0.01 | `consistency.ts` | Повторные доставки от перебалансировки консьюмеров |
| `CORRELATED_FAILURE_SHARE` | 0.05 | `availability.ts` | Доля отказов, общих для всех реплик (плохой деплой, конфиг) |
| `RETRY_BUDGET` | 0.5 | `solver.ts` | Потолок доли ретраев в §3.2 |
| `DETECTION_SEC` / `DNS_TTL_SEC` / `WARMUP_SEC` | 30 / 60 / 60 с | `multiRegion.ts` | Слагаемые RTO; при `failoverMode = manual` добавляется 900 с |
| `HORIZON_DAYS` | 365 сут | `derived.ts` | Горизонт накопления данных |
| `MAX_CALLS_PER_EDGE` | 16 | `latency.ts` | Потолок числа разыгранных вызовов на одно ребро за сэмпл; остаток учитывается масштабированием суммы в `sequential` |
| `QUORUM_ACK_SHARE` | 1 (`sync`) / 0.5 (`semi-sync`) | `latency.ts` | Доля RTT реплики, которую ждёт запись (§7а.3) |
| `DLQ_INGEST_RPS` | 3000 | `components/messaging.ts` | Потолок приёма DLQ как управляемой очереди, помимо ограничителя по глубине |
| `SCALE_UP_LAG_SEC` / `SCALE_DOWN_LAG_SEC` | 90 / 300 с | `transient.ts` | Лаг автоскейлера, если у блока нет своих `scaleUpLagSec` / `scaleDownLagSec`; складывается с `schedulingLagSec` кластера |
| `CACHE_WARMUP_SEC` | 60 с | `transient.ts` | Прогрев узла, у которого hit ratio задан параметром (CDN, DNS), — там нет `M` для формулы `τ = M / λ_read` |
| `CACHE_WARMUP_MIN_SEC` / `CACHE_WARMUP_MAX_SEC` | 20 / 900 с | `transient.ts` | Пределы, в которые зажимается `τ` |
| `BUFFERED_BACKLOG_SEC` | 3600 с | `transient.ts` | Глубина буфера узла за async-ребром: час собственной ёмкости |
| `POISON_REDELIVERY_SEC` / `POISON_MAX_ATTEMPTS` | 30 с / 5 | `transient.ts` | Период повторной доставки ядовитого сообщения и число попыток до DLQ |
| `TIMELINE_ERROR_BUDGET` | 0.01 | `transient.ts` | Доля ошибок, выше которой точка ряда считается нарушением SLO |
| `MAX_TIMELINE_SAMPLES` | 121 | `transient.ts` | Потолок числа точек ряда — держит прогон в десятках миллисекунд |
| `WAIT_TAIL_FACTOR` | `ln(1/0.01) ≈ 4.605` | `transient.ts` | Хвост экспоненциального ожидания при переводе `W_q` в p99 |

Без `CORRELATED_FAILURE_SHARE` три реплики с доступностью 0.999 давали бы девять девяток —
результат, который не выдерживает встречи с реальностью.

### 15.3. Не реализовано

* **`split-brain` не отнимает доступность.** Сценарий (§12) считает расхождение и цену слияния,
  но сторона, оставшаяся без кворума, в модели продолжает обслуживать трафик: в реальности она
  отвечает отказом на запись, и это должно было бы поднять долю ошибок. Разрыв задан стационарно,
  ряда `timeline` у него нет — за 120 с накопленное расхождение выводится числом операций, а не
  кривой.
* **`scaleUpLagSec` / `scaleDownLagSec` заведены только у `service`**: у остальных блоков лаг
  автоскейлера берётся из констант движка (§15.2), задать его на узле нельзя.
* **`lost-update` и `duplicate-processing`** (§12) остаются частью модели аномалий и отдельными
  сценариями не заведены.
* **Ряд `timeline` не участвует в приёмке заданий** (`docs/04-challenges.md`): предикаты
  по-прежнему смотрят на стационарный срез, который для transient-сценария берётся в худшей точке
  воздействия.
* **Гарантия порядка задаётся не везде** (A7, §7а.2): `orderingScope` есть только у Kafka,
  `queueType` — только у SQS. RabbitMQ, NATS, Kinesis, Redis Streams и SNS считаются
  неупорядоченными без возможности заявить обратное, даже если в реальной постановке порядок
  обеспечен ключом партиционирования на стороне продюсера.
* **A8 не различает три свои аномалии по частоте**: грязное, неповторяющееся и фантомное чтение
  выводятся с одной оценкой сверху и признаком `upperBound`. Разделить их — значит завести модель
  длительности транзакции и её фаз, которой нет: окно берётся как `serviceSec · 2`.
* **`deliverySemantics` на async-ребре** (`at-most-once` / `at-least-once` / `effectively-once`):
  поля в `EdgePolicy` нет, дубликаты A6 всегда считаются по семантике `at-least-once`, а гасит их
  только идемпотентность ребра или консьюмера.
* **Инкрементальный пересчёт** по хэшу подграфа (§13): пересчитывается вся схема целиком.
  На демо-схемах это 40–60 мс, поэтому пока не мешает.
* **Автоскейлинг** реализован у `service` (по целевой утилизации) и у `ws-gateway` (по числу
  соединений) — у остальных блоков число инстансов задаётся руками.
* **Рост кластера ничем не ограничен сверху.** При `autoscaleNodes = true` `k8s-cluster`
  доращивает ноды под любое число подов: квоты облака, свободные адреса в подсети и наличие
  инстансов нужного типа не моделируются. Параметра-потолка нет ни в одном документе, поэтому он
  не заведён; если заводить — имя `maxNodes` уже занято ограничением схемы в заданиях
  (`challenge.constraints.maxNodes`), и брать его нельзя.
* **Ожидание зависимости не входит во время ожидания в очереди.** Ёмкость вызывающего оно уже
  режет (§4.а), а формула Сакасэгавы (§5.1) по-прежнему считает ожидание от собственного `S`
  блока, а не от времени удержания слота `S + S_зав`.
* **Поправка на порядковую статистику опирается на логнормальность плеча** (§7): при
  `fanout > 16` и `callMode = parallel` максимум досчитывается по асимптотике максимума
  логнормальной величины, а не разыгрывается честно. Для смеси «быстрый кэш плюс редкий промах»
  это оценка сверху по разбросу, а не точная цифра.
* **Падение write-throughput от синхронной репликации** (§7а.3): ожидание подтверждения попадает
  в latency записи, но не уменьшает ёмкость хранилища — для этого нужно, чтобы `serviceSec` блока
  зависел от `replicationMode`.

### 15.4. Как проверяется модель

Из семи пунктов §14 реализованы четыре, тестами в `tests/engine/`:

| Проверка | Где | Что закреплено |
|---|---|---|
| Сверка с теорией очередей | `model-validation.test.ts` | `W_q` для M/M/1 совпадает с точной формулой `ρS/(1−ρ)`, расхождение < 5% |
| Предельные случаи | `model-validation.test.ts` | ρ→0 ⇒ `W_q`→0; рост числа приборов снижает ожидание; при ρ→1 ожидание упирается в размер очереди; `M ≥ N` ⇒ `h = 1` |
| Табличные значения кэша | `model-validation.test.ts` | `h ≈ 0.67` при `N = 10⁹`, `M = 10⁶` — строка из таблицы §6 |
| Детерминизм | `simulation.test.ts` | Один seed — одна и та же p99 |
| Приёмка Definition of Done | `demo-schemes.test.ts` | Демо-схемы «Видеоплатформа» и «Платежи в двух регионах» |
| Объяснимость | `capacity-coverage.test.ts` | У каждого блока есть модель; каждый ограничитель имеет формулу, подставленные значения и перевод |
| Честность модели | `model-honesty.test.ts` | `fanout` стоит задержки, плечи балансировки не складываются, наполнение CDN из origin оплачивается, насыщение портит хвост раньше доли ошибок, синхронная репликация стоит RTT на записи, ёмкость DLQ, резидентность против репликации внутри юрисдикции |

**12 золотых схем с ручным расчётом (§14, пункт 1) не написаны** — вместо них пока две демо-схемы,
служащие приёмочным тестом фазы. Проверки размерности (§14, пункт 2) и сверка дефолтов с публичными
бенчмарками (§14, пункт 5) не автоматизированы.
